# macOS
.DS_Store
.AppleDouble
.LSOverride

# SVG source (chỉ cần PNG)
icons/*.svg

# Editor
.vscode/
.idea/

.sample

# Node tooling (devDependencies only — extension itself is dependency-free)
node_modules/
npm-debug.log*

# Build artifacts (generated by release workflow)
studocu-tools-v*.zip

# web-ext
web-ext-artifacts/
